<?php $this->headTitle($this->marque); ?>
<div>
    <h2><b><?= $this->marque?></b></h2>
    <?php
    foreach($this->marquedata as $model) {

        $url = $this->serverUrl($this->url('carmodels',['marque' => $this->marque, 'model' => $model]));
        echo '<br />'.    '<a href="'.$url.'">'.$model.'</a>';
    }
    ?>
</div>